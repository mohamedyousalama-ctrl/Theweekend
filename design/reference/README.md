# Design reference — owner-supplied Claude Design export (immutable)

| Field | Value |
|---|---|
| File | `rakan-guest-memory.dc.html` (63,838 bytes) |
| SHA-256 | `7ee86b47bd1b75f21655e30bd2a2770b8a9f4bea8ce1e50af7982f0a80b0d980` (see `SHA256SUMS`) |
| Supplied by | the repository owner, 2026-09-13, as the visual source of truth for issue #6 |
| Custody | stream C. **Do not edit this file.** A changed hash means a different design; new versions are added as new files with their own hash. |
| Status label | `owner-supplied design reference` — not `merchant approved` data, not executable production UI |

## What it contains

One Claude Design canvas (`text/x-dc` DCLogic script + HTML/CSS) with the Arabic, RTL "guest memory" screens: kiosk check-in, barber push notification, guest card, post-service capture, guest list. Dark navy ground (`#07090F`, `#0A0D15`, `#0E121C`), brand red `#E11D2E`, gold accent `#F2B03D`, greys `#8F98AB` / `#9AA3B5` / `#767F92` / `#6E7689`, light text `#F4F6FA`. Fonts referenced by URL: IBM Plex Sans Arabic (300–700) and Archivo Black from Google Fonts. Wordmark text: THE WEEKEND.

## Inspection result (before archiving)

- No images, no base64 assets, no phone numbers, emails, tokens, keys or customer data.
- External references: Google Fonts stylesheet only. **No font files are committed** (license: use the font references, do not publish container font files).
- `./support.js` is referenced but was **not** supplied; the DCLogic runtime is not in this repository. The file therefore does not run as-is. Port the behaviour into the accepted application components (issue #6); do not fabricate a working export.
- All people, branches, prices, visit counts, waits, ratings and "health notes" in the file are **synthetic**. One synthetic guest is named «خالد العتيبي»; this coincides with the owner's first name only by accident and must not be treated as a real person or record.

## Behaviours in this file that must NOT be ported (issue #6, #2)

Entrance face-recognition simulation; forced front-photo capture; all-branch public guest list; fake visit counts / estimated waits / preselected "very satisfied" rating; browser-only "saved" success; bundling photo sharing into book/save actions. Keep the appearance; correct the behaviour.
