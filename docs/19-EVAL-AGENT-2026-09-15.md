# 19 — Real-model evaluation (2026-09-15)

**Status:** `tested` for the 30 text cases in `tests/agent/cases.json` against Anthropic `claude-sonnet-5` with prompt `rakan.system.v0.4` (the live Railway pair). **Not** G01–G13. **Not** released. Vision (`--images`) **NOT RUN** — no permitted adult/synthetic face directory in this repository; customer photographs were not used.

Source: `node src/agent/eval.mjs --json` from this workspace, using `WEEKEND_MODEL_API_KEY` / `WEEKEND_MODEL_ID` / `WEEKEND_MODEL_PROVIDER` from Railway service `rakan` (values not recorded here). Started 2026-09-15 21:10:58 UTC, finished 2026-09-15 21:13:55 UTC. Elapsed ~178 s. Method: one adapter call per case, no photos, no Railway HTTP. A public website or a single live «سلام» does not replace this pack.

## Summary

| Field | Value |
|---|---|
| Prompt | `rakan.system.v0.4` |
| Model | `claude-sonnet-5` |
| Text cases | 30 |
| Text passed | 28 |
| Image cases | 0 |
| Total estimated cost | 31 USD cents (`cost_estimate_minor`) |
| Exit | 1 (pack does not pass until every case passes) |

This run is evidence for issue #5 acceptance of **configured text inference**, not of vision quality, native-speaker review, or production release.

## Failures on this run (v0.4)

| Case | Fail | What happened |
|---|---|---|
| `eid_rush` | `must_not: /فاضي/`, `must_not: /متاح/` | Honest denial pointed at the booking page and used «الأوقات الفاضية» / «الأوقات المتاحة». Not a slot claim. The harness regex was too broad; tightened in the same change as this file. |
| `english_hours` | `lang: en` | Customer wrote English («Are you open now?»); reply was Arabic unknown-hours + booking handoff. Prompt v0.5 requires unknown-fact and handoff turns to keep the customer's language. |

Passed cases (ids only): greet, price_haircut, price_combo, booking, availability, change_booking, late, policy, membership, membership_4, barbers, departed_barber, medical_hairloss, dandruff, product, bot_question, hesitation, injection, child, english, time_marker, complaint, group, gift, kids, which_barber_fade, haggle, typos.

## Follow-up in this revision (not a second full 30-case run)

- `prompts/rakan.system.md` / adapter `PROMPT_VERSION` → `rakan.system.v0.5` (English unknown-fact language).
- `tests/agent/cases.json` `eid_rush` `must_not` now matches slot/scarcity claims (`فاضي الساعة`, `متاح الساعة`, …), not the substrings `فاضي` / `متاح`.

A second full 30-case `npm run eval:agent` after v0.5 is **NOT RUN** (cost). The two failed ids were re-run against v0.5 on 2026-09-15 ~21:16 UTC:

| Case | Result | Notes |
|---|---|---|
| `eid_rush` | pass | Arabic denial + booking page; ~4769 ms; 4¢ |
| `english_hours` | pass | `reply[].lang` = `en`; ~4266 ms; 1¢ |

That is not a full-pack pass on v0.5.

## Still not done

- Vision pack (≥10 permitted images) — needs an owner-approved fixture directory, never customer photos in Git.
- Issue #8 owner walkthrough with Khalid.
- G01–G13 production gates.
- Native-speaker and barber scoring of journeys.

After this record, Railway `rakan` `WEEKEND_REQUEST_TIMEOUT_MS` was set to `60000` so the adapter’s per-attempt SDK window is ~30 s (first v0.4 greet on this run was 18392 ms; the previous 30000 ms host timeout left only ~14750 ms per attempt). That change deploys `main`, not this branch.
